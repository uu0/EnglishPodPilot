# 含 API 密钥，禁止提交
config.json

# Python 缓存
__pycache__/
*.pyc

# 运行时用户数据（进度/多用户数据库）
app.db
app.db-*

# macOS
.DS_Store

# 大体积离线资源不入库：音频 5.2G + 词典 176M（ecdict.db 113M 超 GitHub 单文件限制）
# 字幕/文本/PDF 已入库（srt ~10M / txt ~6M / pdf ~34M）
data/audio/*
data/dict/*
!data/audio/.gitkeep
!data/dict/.gitkeep
